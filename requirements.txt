sentence-transformers==3.4.1
chromadb>=1.2.0  # 1.1.x panics on some SQLite versions (Rust binding bug)
groq==0.15.0
python-dotenv==1.0.1

# Query interface — Milestone 5:
gradio>=6.9.0

# Document ingestion — add if your documents/ folder contains .pdf files:
# pdfplumber==0.11.4
